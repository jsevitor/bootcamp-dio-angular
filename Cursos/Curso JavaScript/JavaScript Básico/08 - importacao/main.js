/*
// const funcoes = require('./funcoes_auxiliares');
const { gets, print } = require('./funcoes_auxiliares');

print(gets());


// Object Destructing - Exemplo
const pessoa = {
    nome: 'Vitor'
}

const { nome } = pessoa;
// const { nome } = pessoa.nome;
*/

const { gets, print } = require('./funcoes_auxiliares');

print(gets());
print(gets());
print(gets());
print(gets());
print(gets());
print(gets());